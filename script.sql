crie o script de insert para inserir na tabela abaixo CREATE TABLE Veiculo ( Id INT PRIMARY KEY AUTO_INCREMENT, Placa VARCHAR(10) NOT NULL UNIQUE, Chassi VARCHAR(17) NOT NULL UNIQUE, Renavam VARCHAR(11) NOT NULL UNIQUE, Modelo VARCHAR(100) NOT NULL, Marca VARCHAR(100) NOT NULL, Ano INT NOT NULL );